#!/usr/bin/env python

env = SConscript("godot-cpp/SConstruct")

env.Append(CPPPATH = [Dir('subprocess').abspath])

if env['platform'] == 'windows':
    env.Append(CPPPATH = [Dir('windows').abspath])

Return('env')